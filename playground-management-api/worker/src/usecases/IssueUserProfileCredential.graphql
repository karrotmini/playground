mutation IssueUserProfileCredential(
  $userProfileId: ID
  $permission: UserProfilePermision!
) {
  issueUserProfileCredential(input: {
    userProfileId: $userProfileId
    permission: $permission
  }) {
    credential
    userProfile {
      id
    }
  }
}
